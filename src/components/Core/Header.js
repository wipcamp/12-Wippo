import React, { Component } from 'react'
import { Layout } from 'antd'

const { Header } = Layout;

export default class MainHeader extends Component {
  render() {
    return (
      <Header>This is Header Ja.</Header>
    )
  }
}
